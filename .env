# Offers service environment
# Use the shared MariaDB database (same as Login)
DATABASE_URL=mariadb+mariadbconnector://root:mypass@127.0.0.1:3306/pupero_auth
# Port the Offers FastAPI will listen on
OFFERS_PORT=8001
